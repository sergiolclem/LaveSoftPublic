import { RealDirective } from './real.directive';

describe('RealDirective', () => {
  it('should create an instance', () => {
    const directive = new RealDirective();
    expect(directive).toBeTruthy();
  });
});
