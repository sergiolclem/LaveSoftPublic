import { PorcentagemDirective } from './porcentagem.directive';

describe('PorcentagemDirective', () => {
  it('should create an instance', () => {
    const directive = new PorcentagemDirective();
    expect(directive).toBeTruthy();
  });
});
